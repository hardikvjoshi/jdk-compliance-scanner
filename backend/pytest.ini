[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --cov=core
    --cov=api
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
asyncio_mode = auto

